{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}

 <div class="container login">

        <!-- Start Logo -->
    <!-- Start Login Form -->
        <div class="row justify-content-center">         
            <div class="col-md-6">
                <h1 class="mb-5 text-center">Connectez-vous</h1>
                <form class="mb-5" action="{{ path('security_login')}}" method="post">
                    <div class="form-group">
                        <input placeholder="Adresse email..." required name="_username" type="username" class="form-control">
                    </div>
        
                     <div class="form-group mb-2">
                        <input placeholder="Mot de passe..." required name="_password" type="password" class="form-control">
                    </div>

                    <div class="form-group mt-5 text-center">
                        <button type="submit" class="btn btn-success"> connecxion</button>
                    </div>
                </form>

                <!-- TODO lien Mot de passe oubliée!
                 <div class="text-center">
                   
                </div>  -->

                </div>
        </div>
        <!-- Start Login Form -->
        
    </div>
   {% endblock %}